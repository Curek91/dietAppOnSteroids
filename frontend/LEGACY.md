# LEGACY — Angular frontend

**Status:** zamrożone. Nie rozwijamy, nie deployujemy.

Stary frontend w Angularze. Zostawiony jako referencja na czas migracji do
Next.js (`apps/web`). Wszystkie nowe ekrany powstają w `apps/web/src/app`.

Jeśli zaczynasz nową pracę nad UI — **nie tutaj**. Idź do:
- `apps/web/` (Next.js, web + landing)
- `apps/mobile/` (Expo, iOS + Android)

Po ukończeniu migracji ten folder zostanie usunięty z repo (zob. `docs/ROADMAP.md`).
