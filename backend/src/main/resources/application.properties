spring.application.name=trainer-backend
server.port=${PORT:8080}

# Active Spring Profile (set to 'h2' or 'oracle')
# Defaulting to 'h2' so the application runs immediately without requiring an Oracle setup.
# Change to 'oracle' to connect to a local or remote Oracle 23c database.
spring.profiles.active=h2
